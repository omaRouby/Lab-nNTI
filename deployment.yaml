apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab-app-deployment
  labels:
    app: lab-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lab-app
  template:
    metadata:
      labels:
        app: lab-app
    spec:
      containers:
        - name: lab-app
          image: omarrouby/lab-app-python
          ports:
            - containerPort: 80
